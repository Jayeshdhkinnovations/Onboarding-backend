meta {
  name: Publish Form
  type: http
  seq: 6
}

post {
  url: {{base_url}}/api/forms/:formId/publish
  body: none
  auth: inherit
}

params:path {
  formId: {{formId}}
}

assert {
  res.status: eq 200
}
