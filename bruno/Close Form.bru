meta {
  name: Close Form
  type: http
  seq: 7
}

post {
  url: {{base_url}}/api/forms/:formId/close
  body: none
  auth: inherit
}

params:path {
  formId: {{formId}}
}

assert {
  res.status: eq 200
}
