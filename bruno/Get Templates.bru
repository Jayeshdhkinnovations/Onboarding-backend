meta {
  name: Get Templates
  type: http
  seq: 12
}

get {
  url: {{base_url}}/api/templates
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body.success: eq true
  res.body.data: isArray
}
