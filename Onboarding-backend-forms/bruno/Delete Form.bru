meta {
  name: Delete Form
  type: http
  seq: 9
}

delete {
  url: {{base_url}}/api/forms/:formId
  body: none
  auth: inherit
}

params:path {
  formId: {{formId}}
}

assert {
  res.status: eq 200
}
